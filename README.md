# Investment Dashboard

Read-only Streamlit deployment exported from the private source repository.

## Required secrets

- `SUPABASE_URL`
- `SUPABASE_ANON_KEY`

The app defaults to `APP_MODE=public`, `market`, and `portfolio`.
Those values do not need to be stored as secrets.

This repo is generated. Make source changes in the private repository.
